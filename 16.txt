前情回顾

1. 并发网络模型

   多进程并发模型
   多线程并发模型

2. ftp文件服务器

   * 面向对象思想，并发模型编写服务
   * 请求和应答
     网络协议————》 请求   响应（OK FAIL）

3. IO

   输入(读取)  输出(写入)

   IO密集型： IO多  耗时长  多阻塞  cpu消耗少
   计算密集： 计算多 cpu消耗多  无阻塞  耗时短

cookie:
   Linux系统中对每个IO对象会分配的整数编号（>=0）
   这个整数编号就是文件描述符

   fd.fileno() 获取文件描述符

练习01：
将select_server.py改写为使用epoll方法实现


作业： 1. 独立使用select 和 epoll搭建并发模型



